في السطر 60-71 من MediaFullscreenView.tsx، استبدل:

        } else if (metadata.source_type === 'google_drive' && metadata.drive_id) {
          return (
            <div className="w-full h-full flex items-center justify-center bg-black">
              <iframe
                src={`https://drive.google.com/file/d/${metadata.drive_id}/preview`}
                title={media.file_name}
                className="w-full h-full"
                frameBorder="0"
                allow="autoplay"
              />
            </div>
          );
        }

بهذا:

        } else if (metadata.source_type === 'google_drive') {
          const driveId = extractGoogleDriveId(metadata, media.file_path);
          
          if (driveId) {
            return (
              <div className="w-full h-full flex items-center justify-center bg-black">
                <iframe
                  src={`https://drive.google.com/file/d/${driveId}/preview`}
                  title={media.file_name}
                  className="w-full h-full"
                  frameBorder="0"
                  allow="autoplay"
                />
              </div>
            );
          }
        }
